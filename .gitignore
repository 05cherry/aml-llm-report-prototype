# ── 보안 (절대 커밋 금지) ──
.env
.env.*
!.env.example

# ── Python ──
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── PyCharm / JetBrains ──
.idea/

# ── OS ──
.DS_Store
Thumbs.db

# ── Claude Code (로컬 개인 설정만 제외) ──
# .claude/commands/ 와 settings.json 은 커밋한다. 로컬 오버라이드만 제외.
.claude/settings.local.json.claude/skills/
.claude/CLAUDE.md
